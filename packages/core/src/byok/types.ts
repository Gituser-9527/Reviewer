export type LLMProviderType = 'OPENAI' | 'OPENAI_COMPATIBLE' | 'AZURE_OPENAI' | 'ANTHROPIC' | 'GOOGLE' | 'LOCAL_OPENAI_COMPATIBLE';
export interface LLMConnectionConfig { id: string; tenantId: string; displayName: string; provider: LLMProviderType; baseUrl?: string; encryptedApiKey: import('./secret-encryption.js').EncryptedSecret; models: Partial<Record<'extraction' | 'classification' | 'audit' | 'review' | 'rewrite' | 'embedding', string>>; options: { timeoutMs: number; maxOutputTokens: number; temperature: number; maxRetries: number; reasoningEffort?: 'none' | 'minimal' | 'low' | 'medium' | 'high' }; status: 'ACTIVE' | 'DISABLED' | 'INVALID' | 'RATE_LIMITED' | 'UNVERIFIED'; isDefault: boolean; createdAt: string; updatedAt: string; }
