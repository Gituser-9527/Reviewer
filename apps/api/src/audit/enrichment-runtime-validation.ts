import { auditExplanationResultSchema, jobPostingRewriteResultSchema } from '@job-compliance/core';
import { validateRewriteSafety, type ProtectedJobFacts } from './rewrite-safety.js';

export function validateExplanationRuntime(value:unknown,auditRunId:string,findingIds:ReadonlySet<string>,evidenceIds:ReadonlySet<string>):{ok:true;value:unknown}|{ok:false;code:string}{const parsed=auditExplanationResultSchema.safeParse(value);if(!parsed.success)return{ok:false,code:'EXPLANATION_SCHEMA_VALIDATION_FAILED'};if(parsed.data.auditRunId!==auditRunId)return{ok:false,code:'EXPLANATION_UNGROUNDED_CONTENT'};for(const finding of parsed.data.findings){if(!findingIds.has(finding.findingId))return{ok:false,code:'EXPLANATION_UNKNOWN_FINDING'};if(finding.evidenceRefs.some(id=>!evidenceIds.has(id)))return{ok:false,code:'EXPLANATION_UNKNOWN_EVIDENCE'};}return{ok:true,value:parsed.data};}
export function validateRewriteRuntime(value:unknown,facts:ProtectedJobFacts,findingIds:ReadonlySet<string>):{ok:true;value:unknown;safety:unknown}|{ok:false;code:string;safety:unknown}{const parsed=jobPostingRewriteResultSchema.safeParse(value);if(!parsed.success)return{ok:false,code:'REWRITE_SCHEMA_VALIDATION_FAILED',safety:{passed:false,decision:'REJECTED'}};const checked=validateRewriteSafety(parsed.data,facts,findingIds);return checked.safety.passed?{ok:true,value:checked.rewrite!,safety:checked.safety}:{ok:false,code:checked.safety.protectedFactViolations.includes('UNKNOWN_FINDING_REFERENCE')?'REWRITE_UNKNOWN_FINDING':'REWRITE_SECONDARY_REVIEW_FAILED',safety:checked.safety};}
